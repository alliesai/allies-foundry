# Plan draft to clean copy

The draft is preserved at
`fnd-008-5-long-stream-completion-plan-draft.md`. The clean copy is
`fnd-008-5-long-stream-completion-plan-edited.md`.

Edits made with the better-docs and humanizer pass:

- Replaced the generic title with the reader-facing decision: "let long Hermes
  streams finish."
- Tightened the success statement and the resource-boundary language without
  changing any requirement, number, scope boundary, or acceptance criterion.
- Removed a few abstract or repetitive phrases and used direct subjects and
  verbs.
- Kept technical names, event names, limits, dates, evidence paths, and the
  rollback condition unchanged.
- Checked the clean copy for em dashes, en dashes, promotional language,
  unsupported facts, and changes to `must`, `should`, `may`, `never`, or
  `always` commitments.
